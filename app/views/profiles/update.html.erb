def update
  if current_user.update(profile_params)
    redirect_to new_family_path
  else
    render :edit, status: :unprocessable_entity
  end
end

private

def profile_params
  params.require(:user).permit(:nickname, :character)
end
